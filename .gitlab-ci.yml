# variables:
#   IMAGE_NAME: nanajanashia/demo-app
#   IMAGE_TAG: python-app-1.0

stages:
  - test
  - build
  # - deploy

run_tests:
  stage: test
  image: python:3.9-slim
  before_script:
    - apt-get update && apt-get install make -y
  script:
    - make test

build_image:
  stage: build
  image: docker:20.10.16
  services:
    - docker:20.10.16-dind
  variables:
    DOCKER_TLS_CERTDIR: "/certs"
  before_script:
    - apk update && apk add --no-cache curl zip unzip
    - curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "/tmp/awscliv2.zip"
    - unzip /tmp/awscliv2.zip
    - ./aws/install
  #   - docker login -u $REGISTRY_USER -p $REGISTRY_PASS
  script:
    - echo we will run docker login here.
    # - docker build -t $IMAGE_NAME:$IMAGE_TAG .
    # - docker push $IMAGE_NAME:$IMAGE_TAG

# deploy:
#   stage: deploy
#   before_script:
#     - chmod 400 $SSH_KEY
#   script:
#     - ssh -o StrictHostKeyChecking=no -i $SSH_KEY root@161.35.223.117 "
#         docker login -u $REGISTRY_USER -p $REGISTRY_PASS &&
#         docker ps -aq | xargs docker stop | xargs docker rm &&
#         docker run -d -p 5000:5000 $IMAGE_NAME:$IMAGE_TAG"